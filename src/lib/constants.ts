// ── LocalStorage key names ────────────────────────────────
// Single source of truth — never hardcode these in components.
export const STORAGE_KEYS = {
  FOLDERS:    'devos_folders',
  NOTES:      'devos_notes',
  STICKIES:   'devos_stickies',
  ACTIVITIES: 'devos_activities',
} as const;
